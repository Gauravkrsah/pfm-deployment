% ============================================================
% Preamble - Document Setup and Formatting
% Personal Finance Manager (PFM) Academic Report
% ============================================================

% ------------------------------------------------------------
% ESSENTIAL PACKAGES
% ------------------------------------------------------------

% Font: Times New Roman equivalent
% Using mathptmx as it's more commonly available than newtxtext
\usepackage{mathptmx}
\usepackage[T1]{fontenc}

% Page geometry (Left: 1.25", Others: 1")
\usepackage[
    left=1.25in,
    right=1in,
    top=1in,
    bottom=1in
]{geometry}

% Line spacing: 1.5
\usepackage{setspace}
\onehalfspacing

% Justified text (default in LaTeX)
\usepackage{ragged2e}

% Graphics and figures
\usepackage{graphicx}
\graphicspath{{figures/}}

% Tables
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{multirow}
\usepackage{tabularx}

% Colors
\usepackage[dvipsnames,table]{xcolor}

% Code listings
\usepackage{listings}
\lstset{
    basicstyle=\ttfamily\small,
    breaklines=true,
    frame=single,
    numbers=left,
    numberstyle=\tiny\color{gray},
    keywordstyle=\color{blue},
    commentstyle=\color{green!60!black},
    stringstyle=\color{red},
    showstringspaces=false,
    tabsize=2
}

% Define JavaScript language
\lstdefinelanguage{JavaScript}{
  keywords={typeof, new, true, false, catch, function, return, null, catch, switch, var, if, in, while, do, else, case, break, const, let, async, await, export, default, import, from},
  keywordstyle=\color{blue}\bfseries,
  ndkeywords={class, export, boolean, throw, implements, import, this},
  ndkeywordstyle=\color{darkgray}\bfseries,
  identifierstyle=\color{black},
  sensitive=false,
  comment=[l]{//},
  morecomment=[s]{/*}{*/},
  commentstyle=\color{green!60!black}\ttfamily,
  stringstyle=\color{red}\ttfamily,
  morestring=[b]',
  morestring=[b]"
}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=blue,
    urlcolor=blue,
    pdfauthor={Gaurav Kumar Sah},
    pdftitle={Personal Finance Manager - Project Report}
}

% Bibliography
\usepackage{cite}

% Captions
\usepackage{caption}
\captionsetup{
    font=small,
    labelfont=bf,
    justification=centering
}

% Headers and footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% For plain pages (chapter starts)
\fancypagestyle{plain}{
    \fancyhf{}
    \fancyfoot[C]{\thepage}
    \renewcommand{\headrulewidth}{0pt}
}

% ------------------------------------------------------------
% CHAPTER AND SECTION FORMATTING
% ------------------------------------------------------------

\usepackage{titlesec}

% Chapter: 16pt, Bold, Center
\titleformat{\chapter}[display]
    {\normalfont\fontsize{16}{19}\bfseries\centering}
    {\chaptertitlename\ \thechapter}
    {0pt}
    {\fontsize{16}{19}\selectfont}
\titlespacing*{\chapter}{0pt}{-20pt}{20pt}

% Section: 14pt, Bold, Left
\titleformat{\section}
    {\normalfont\fontsize{14}{17}\bfseries}
    {\thesection}
    {1em}
    {}

% Subsection: 12pt, Bold, Left
\titleformat{\subsection}
    {\normalfont\fontsize{12}{14}\bfseries}
    {\thesubsection}
    {1em}
    {}

% Subsubsection: 12pt, Bold, Left
\titleformat{\subsubsection}
    {\normalfont\fontsize{12}{14}\bfseries}
    {\thesubsubsection}
    {1em}
    {}

% ------------------------------------------------------------
% CUSTOM COMMANDS
% ------------------------------------------------------------

% Project details (edit these)
\newcommand{\projecttitle}{Personal Finance Manager (PFM)}
\newcommand{\studentname}{Gaurav Kumar Sah}
\newcommand{\studentid}{[Your Student ID]}
\newcommand{\supervisorname}{[Supervisor Name]}
\newcommand{\supervisortitle}{[Supervisor Title/Designation]}
\newcommand{\collegename}{[College Name]}
\newcommand{\universityname}{[University Name]}
\newcommand{\departmentname}{Department of Computer Application}
\newcommand{\degreename}{Bachelor in Computer Application (BCA)}
\newcommand{\submissiondate}{January 2026}
\newcommand{\academicyear}{2025/2026}

% Blank line command
\newcommand{\blankline}{\vspace{\baselineskip}}

% Signature line
\newcommand{\signatureline}[1]{%
    \vspace{1cm}
    \noindent\rule{6cm}{0.4pt}\\
    #1
}

% ------------------------------------------------------------
% FLOAT PLACEMENT
% ------------------------------------------------------------
\usepackage{float}

% Prevent figures from floating to other sections
\usepackage[section]{placeins}

% ------------------------------------------------------------
% PDF METADATA
% ------------------------------------------------------------
\usepackage{pdfpages}
